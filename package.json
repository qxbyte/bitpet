{
  "name": "bitpet",
  "version": "1.0.3",
  "description": "A desktop pet companion for CLI AI tools",
  "scripts": {
    "sync-version": "node scripts/sync-version.js",
    "dev": "npm run sync-version && tauri dev",
    "build": "npm run sync-version && tauri build",
    "release": "node scripts/sync-version.js && cd cli && npm publish",
    "tauri": "tauri",
    "vite:dev": "vite",
    "vite:build": "vite build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "typescript": "^5.0.0",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  }
}
